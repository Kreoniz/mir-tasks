import { MirTooltipDirective } from './mir-tooltip.directive';

describe('MirTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new MirTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
