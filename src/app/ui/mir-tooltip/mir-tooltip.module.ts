/*
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MirTooltipDirective } from './mir-tooltip.directive';
import { MirTooltipComponent } from './mir-tooltip.component';

@NgModule({
  imports: [CommonModule],
  declarations: [MirTooltipDirective, MirTooltipComponent],
  exports: [MirTooltipDirective],
})
export class MirTooltipModule {}
*/
